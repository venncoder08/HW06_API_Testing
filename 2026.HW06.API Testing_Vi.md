# HW06 - Kiểm thử API

## 1. Thông tin chung

| **Mã bài tập** | **HW06-AI** |
| --- | --- |
| **Thời lượng** | 10 giờ |
| **Hạn nộp** | Vui lòng xem liên kết nộp bài trên Moodle |
| **Hình thức** | Bài tập cá nhân |
| **Nộp bài** | Moodle (báo cáo) |
| **Giảng viên & Trợ giảng** | TS. Lâm Quang Vũ / TS. Trần Duy Hoàng / ThS. Trần Thị Bích Hạnh / ThS. Trương Phước Lộc / ThS. Hồ Tuấn Thanh |
| **Liên hệ** | lqvu@fit.hcmus.edu.vn / tdhoang@fit.hcmus.edu.vn / ttbhanh@fit.hcmus.edu.vn / tploc@fit.hcmus.edu.vn / hthanh@fit.hcmus.edu.vn |
| **Chính sách AI** | Mở - bắt buộc phải có phần khai báo và Báo cáo Kiểm toán AI đính kèm |
| **Mức Bloom-AI yêu cầu** | G9.1 -> G9.6, tùy theo bài tập (xem phần *Ánh xạ CLO*) |

## 2. Nguyên tắc hướng dẫn

Các nguyên tắc này quy định cách bạn được kỳ vọng làm việc trong toàn bộ chuỗi bài tập của học phần. Hãy đọc kỹ trước khi bắt đầu, vì bài nộp của bạn sẽ được đánh giá dựa trên các nguyên tắc này.

- **Chiến lược AI-First.** Bạn bắt buộc phải áp dụng AI vào các kỹ thuật kiểm thử đã học trên lớp. Tuy nhiên, điều này không có nghĩa là chỉ đưa một prompt chung chung như *"generate all the API test cases from the spec and run them."* Thay vào đó, bạn phải hướng dẫn AI qua từng bước của kỹ thuật như đã được giảng dạy, sử dụng AI như một trợ lý có kỷ luật thay vì một hộp đen.
- **Rà soát của con người.** Mọi kết quả do AI tạo ra phải được chính bạn, sinh viên, rà soát cẩn thận. Bạn hoàn toàn chịu trách nhiệm về tính đúng đắn của các kết quả này. Bạn được kỳ vọng thực hiện mọi chỉnh sửa và tinh chỉnh cần thiết - nộp nguyên văn đầu ra thô của AI mà không rà soát là không chấp nhận được.
- **Báo cáo Kiểm toán AI.** Toàn bộ quá trình sử dụng AI phải được ghi lại trong một nhật ký đầy đủ. Bạn được khuyến khích xây dựng Agent Skills có thể tự động thực hiện các hoạt động này trên các bài tập tương tự. Nếu bạn **không** sử dụng AI, bạn vẫn phải khai báo rõ điều đó.
- **Tài liệu hóa.** Toàn bộ quy trình làm việc phải được tài liệu hóa ở định dạng dựa trên văn bản, chẳng hạn Markdown.
- **Chất lượng quan trọng hơn hoàn thành hình thức.** Bài làm của bạn sẽ được chấm không chỉ dựa trên việc có hoàn thành hay không, mà còn dựa trên số lượng và chất lượng của các sản phẩm bàn giao: ca kiểm thử, kiểm toán AI, Postman collection và báo cáo Newman, báo cáo lỗi, thiết kế bộ sinh kiểm thử, và các liên kết tham chiếu.

## 3. Kết quả học tập

Sau khi hoàn thành bài tập này, bạn sẽ có thể:

- Sử dụng AI để sinh ca kiểm thử API từ đặc tả API của SUT, sau đó kiểm toán và mở rộng các ca kiểm thử đó.
- Thiết kế kiểm thử API bao phủ phân hoạch miền dữ liệu, chuyển trạng thái, bảo mật, và kiểm tra schema.
- Phát hiện các lỗi mà AI bỏ sót, đặc biệt là các lỗi liên quan đến bảo mật và chuyển trạng thái.
- Thiết kế một bộ sinh kiểm thử vận hành bằng AI cho SUT.
- Thể hiện năng lực Bloom-AI ở các mức **G9.2 (Áp dụng)**, **G9.3 (Phân tích)**, **G9.4 (Cộng tác)**, và **G9.5 (Sáng tạo)**.

## 4. Hệ thống được kiểm thử (SUT)

**SUT:** EShop - một ứng dụng demo thương mại điện tử Việt Nam được thiết kế để thực hành kiểm thử.

**Repository:** https://github.com/ttbhanh/eshop-sut

Các chức năng của ứng dụng được tổ chức thành các nhóm sau:

- **Nhóm A - Xác thực, Danh mục, và Sản phẩm**
    - FR-01: Đăng ký tài khoản
    - FR-02: Đăng nhập và khóa tài khoản
    - FR-03: Quên mật khẩu và đặt lại mật khẩu (hai bước)
    - FR-04: Quản lý hồ sơ cá nhân
    - FR-05: Liệt kê và tìm kiếm sản phẩm
    - FR-06: Xem chi tiết sản phẩm
- **Nhóm B - Giỏ hàng và Thanh toán**
    - FR-07: Giỏ hàng
    - FR-08: Thanh toán
    - FR-09: Mã giảm giá
    - FR-10: Máy trạng thái đơn hàng
    - FR-11: Xem lịch sử đơn hàng (người dùng)
- **Nhóm C - Quản trị Web**
    - FR-12: Kiểm soát truy cập
    - FR-13: Bảng điều khiển
    - FR-14: Quản lý danh mục (CRUD)
    - FR-15: Quản lý sản phẩm (CRUD)
    - FR-16: Nhập sản phẩm từ CSV
    - FR-17: Quản lý mã giảm giá (CRUD)
    - FR-18: Quản lý đơn hàng (admin)
    - FR-19: Quản lý người dùng (admin)
- **Nhóm D - Ứng dụng di động**

SUT cung cấp đặc tả API trong repository (`api_specification.md`); hãy tham khảo tài liệu này để biết các endpoint có sẵn. Đặc tả cũng định nghĩa các yêu cầu bảo mật **SEC-01-SEC-07**.

## 5. Lựa chọn API

- Chọn **ba (3) API**, mỗi API triển khai một chức năng thuộc **mỗi nhóm A, B, và C** (Nhóm D, ứng dụng di động, không được dùng ở đây vì bài tập này tập trung vào backend API). Tham khảo đặc tả API để tìm các endpoint phía sau từng chức năng đã chọn.
    - **Nhóm A** - ví dụ: đăng nhập (FR-02) hoặc liệt kê / tìm kiếm sản phẩm (FR-05).
    - **Nhóm B** - ví dụ: giỏ hàng (FR-07) hoặc thanh toán / tạo đơn hàng (FR-08, FR-10).
    - **Nhóm C** - ví dụ: một thao tác quản trị sản phẩm hoặc đơn hàng có thay đổi trạng thái (FR-15, FR-18).
- Tương tự các bài tập trước, hãy bảo đảm lựa chọn của bạn **không trùng lặp** giữa các thành viên trong nhóm: không có hai thành viên nào được chọn cùng một bộ ba API.

## 6. Yêu cầu

Với mỗi API trong ba API đã chọn, hãy hoàn thành quy trình sau. Tài liệu hóa quy trình trong báo cáo chính và đính kèm các bằng chứng bắt buộc. Hãy xem lại các bài giảng liên quan đến kiểm thử API trước khi bắt đầu.

1. **Sinh bằng AI.** Cung cấp đặc tả API của SUT cho một công cụ AI và hướng dẫn công cụ đó - từng bước, không dùng một prompt chung chung duy nhất - để sinh ca kiểm thử cho API (mục tiêu **>= 35 ca cho mỗi API**). Các ca kiểm thử phải bao phủ: **phân hoạch miền dữ liệu** trên mọi tham số (ví dụ: định dạng email, độ phức tạp mật khẩu, giá > 0), **chuyển trạng thái** (FR-10: pending -> confirmed -> shipping -> delivered, cùng với quy tắc hủy), **bảo mật** (SEC-01-SEC-07, ví dụ: SQL injection, IDOR, leo thang vai trò), và **kiểm tra schema** (hình dạng phản hồi khớp chính xác với đặc tả).
2. **Kiểm toán (rà soát của con người).** Gán nhãn từng ca kiểm thử do AI sinh là **VALID / INVALID / INCOMPLETE** kèm lý do, và sửa các ca không hợp lệ hoặc chưa hoàn chỉnh. Bạn hoàn toàn chịu trách nhiệm cho bộ ca kiểm thử cuối cùng.
3. **Mở rộng.** Thêm **ít nhất năm** ca kiểm thử do chính bạn tạo mà AI đã bỏ sót - đặc biệt xoay quanh bảo mật và chuyển trạng thái - và giải thích *vì sao* AI bỏ sót chúng (chất lượng prompt, giới hạn của mô hình, hoặc đặc tính của API).
4. **Thực thi.** Chạy các ca kiểm thử bằng Postman + Newman (hoặc Karate / RestAssured). Mọi request phải mang header `X-Student-Id: {StudentID}` (ví dụ: thông qua pre-request script). Tạo báo cáo Newman / HTML.
5. **Báo cáo lỗi.** Báo cáo mọi lỗi thật sự mà bạn tìm thấy - bao gồm các lỗi AI đã bỏ sót - trong báo cáo Markdown và trên trang GitHub Issues của bạn, với ảnh chụp màn hình đính kèm cho từng issue.

Ngoài ra, các yêu cầu kỹ thuật sau áp dụng cho toàn bộ bộ kiểm thử của bạn:

- **Sử dụng càng nhiều tính năng Postman càng hợp lý càng tốt** - ví dụ: workspace, collection, variables, environment, chạy theo dữ liệu (Collection Runner với file dữ liệu), monitors, và mock servers. **Liệt kê các tính năng Postman bạn đã dùng trong báo cáo.** *(Người dùng Karate / RestAssured cung cấp các tính năng công cụ tương đương.)*
- **Tích hợp vào CI/CD.** Thêm các ca kiểm thử API của bạn vào một pipeline CI/CD cho SUT (ví dụ: chạy Newman trong GitHub Actions ở repository của bạn), và viết một **báo cáo CI/CD** ngắn mô tả cấu hình pipeline và hai lần chạy bên dưới, kèm ảnh chụp màn hình và liên kết. Cung cấp **hai commit mẫu**: một commit có pipeline cho thấy **tất cả** ca kiểm thử API đều pass, và một commit có pipeline cho thấy **một** ca kiểm thử fail.

## 7. Agent Skill

- Đối với mức Sáng tạo (G9.5), thiết kế một **bộ sinh ca kiểm thử API vận hành bằng AI** cho SUT: với đầu vào là đặc tả API, nó tự động tạo ca kiểm thử. Cung cấp một **sơ đồ tự vẽ** và **pseudocode** của thiết kế. ("Tự vẽ" nghĩa là bạn tự đưa ra các quyết định thiết kế; có thể dùng bất kỳ công cụ vẽ sơ đồ nào, nhưng bản thân sơ đồ không được AI sinh ra.)
- Bạn được khuyến khích triển khai nó như một Agent Skill có thể tái sử dụng và nộp video minh họa (liên kết YouTube) cho thấy nó sinh kiểm thử cho một API.

## 8. Công cụ được phép và mức Bloom-AI

Bạn có thể sử dụng các công cụ sau, và bạn phải khai báo chúng trong Báo cáo Kiểm toán AI:

- Bất kỳ công cụ AI nào bạn chọn (ví dụ: ChatGPT, Claude, Gemini, Copilot, Cursor).
- Postman + Newman (mặc định) hoặc Karate / RestAssured (thay thế).
- Tùy chọn: các công cụ kiểm thử LLM (Promptfoo, DeepEval, Ragas).

Mức Bloom-AI yêu cầu cho bài tập này là **G9.2 (Áp dụng)**, **G9.3 (Phân tích)**, **G9.4 (Cộng tác)**, và **G9.5 (Sáng tạo)**.

## 9. Báo cáo Kiểm toán AI (Phụ lục bắt buộc)

Đính kèm Báo cáo Kiểm toán AI dưới dạng phụ lục. Sử dụng nội dung của các AI Template được cung cấp nếu cần.

- Nếu bạn không sử dụng AI, khai báo: *"I do not use any AI help in this exercise."*
- Nếu bạn có sử dụng AI, khai báo: *"I use AI tools for the following tasks,"* và bao gồm các thông tin sau cho mỗi lần tương tác:
    - Tên công cụ AI
    - Ngày và giờ
    - Prompt của bạn
    - Đầu ra của AI

Để đơn giản hóa quy trình này, bạn được khuyến khích tạo một skill hoặc rule tự động trích xuất các thông tin trên sau một phiên làm việc với AI.

## 10. Phê bình AI (200-300 từ, bắt buộc)

Viết một đoạn văn 200-300 từ phê bình AI. Trả lời các câu hỏi sau: AI đã sai, thiên lệch, hoặc chưa đầy đủ ở đâu? Vì sao AI không phát hiện được vấn đề? Bạn đã học được nguyên tắc gì về cộng tác với AI trong bài tập này?

Sử dụng nội dung của các AI Template được cung cấp nếu cần.

## 11. Ràng buộc chống gian lận bằng AI

Bài tập này dựa trên bằng chứng thực thi thật, có thể truy vết. Các nội dung sau không được AI sinh hoặc bịa đặt, và trợ giảng sẽ kiểm tra khi chấm:

- Header `X-Student-Id: {StudentID}`, được chứng minh bằng ảnh chụp console từ pre-request script của bạn.
- Kết quả chạy Newman, trong đó hostname khớp với deployment của bạn (`localhost` / `127.0.0.1` được chấp nhận).
- Sơ đồ bộ sinh kiểm thử AI, phải là sơ đồ tự vẽ - do bạn thiết kế, không được tạo trực tiếp bởi AI.

## 12. Git Commit Log

- Tạo một Git commit mới cho mỗi bước của quy trình (ví dụ: sinh, kiểm toán, mở rộng, và thực thi cho từng API).
- Cung cấp Git commit log ở định dạng file dựa trên văn bản.

## 13. Vấn đáp

Một nhóm ngẫu nhiên gồm **30% sinh viên** có thể được mời vấn đáp 5-7 phút trong tuần sau hạn nộp, để giải thích cách đã hoàn thành bài tập này.

## 14. Quy định nộp bài

- **Định dạng tên file:** `<StudentID>_HW06_AI_API_<SelfAssessedGrade>.zip`
    - *SelfAssessedGrade:* một số gồm 3 chữ số trong khoảng [000, 100].
    - *Ví dụ:* `25127001_HW06_AI_API_090.zip`
- **Nội dung bắt buộc của file `.zip`:**
    - Báo cáo chính (Markdown + PDF), bao gồm báo cáo kiểm thử API và kiểm toán AI của bạn.
    - Liên kết repository GitHub công khai (collections, scripts, và reports).
    - Postman collection (`.json`) và báo cáo Newman (HTML), cùng danh sách các tính năng Postman bạn đã sử dụng.
    - Một báo cáo CI/CD ngắn: cấu hình pipeline và hai lần chạy pipeline mẫu (một lần tất cả pass, một lần có một ca kiểm thử fail), kèm ảnh chụp màn hình và liên kết.
    - File Excel chứa ca kiểm thử và tổng kết kiểm thử.
    - Sơ đồ và pseudocode của bộ sinh kiểm thử AI (PNG / Mermaid + `.md` / `.py`).
    - Tùy chọn: đặc tả API được chuyển sang OpenAPI (`.yaml` / `.json`); nếu do AI sinh, cũng phải kiểm toán.
    - Báo cáo lỗi, kèm ảnh chụp màn hình các lỗi trên trang GitHub Issues.
    - Phê bình AI và Báo cáo Kiểm toán AI (Markdown + PDF).
    - Git commit log (file văn bản).
    - Một file `README.md` chứa bảng tự đánh giá (bên dưới) và báo cáo tổng kết kiểm thử: số lượng API; số ca kiểm thử được sinh, được thêm, được thực thi, pass, và fail; và số lượng lỗi.
    - Mọi tài liệu hỗ trợ khác.
- Nộp lên Moodle. Về hạn nộp, hãy tham khảo liên kết nộp bài.

## 15. Mẫu đánh giá

| **STT** | **Tiêu chí** | **Điểm** | **Điểm tự đánh giá** |
| --- | --- | --- | --- |
| **1** | API 1 - toàn bộ quy trình (sinh + kiểm toán + mở rộng + thực thi + lỗi) | 30 |  |
| **2** | API 2 - toàn bộ quy trình (cùng tiêu chí) | 30 |  |
| **3** | API 3 - toàn bộ quy trình (cùng tiêu chí) | 30 |  |
| **4** | Agent Skills (bộ sinh kiểm thử vận hành bằng AI) | 10 |  |
|  | **Tổng** | **100** |  |

## 16. Tài liệu tham khảo

- ISTQB Foundation Level Syllabus (ấn bản mới nhất).
- Hardman, P. (2025). *A Post-AI Learning Taxonomy.*
- Fuster Rabella, M. (2025). *OECD Education Working Paper No. 338.*
- Anthropic (2025). *Building Reliable AI Test Agents* - engineering blog.
- Tài liệu DeepEval & Promptfoo - các framework kiểm thử LLM.

## 17. Quy định khác

- **Không cho phép** nộp trễ.
- Thiếu bất kỳ tài liệu bắt buộc nào sẽ bị **0 điểm**.
- Sao chép giữa sinh viên - **bao gồm cả prompt** - sẽ bị **0 điểm cho cả hai bên**.
